{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            {{ form_start(form, {'action' : path('task_create')}) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-success pull-right">Créer la tâche</button>
                <a href="{{ path('task_list') }}" class="btn btn-primary">Retour à la liste des tâches</a>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}