{% extends 'base.html.twig' %}

{% block header_title %}<h1>Inscription</h1>{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            {{ form_start(form, {'action' : path('user_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right">Je m'inscris</button>
        {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
